---
title: "Quick search (legacy)"
description: "Deprecated alias of the search endpoint. Use GET /v1/search for new integrations."
openapi: "GET /v1/quick_search"
---

<Warning>
  `GET /v1/quick_search` is a deprecated alias of [`GET /v1/search`](/api-reference/endpoints/search) and is scheduled for removal on **2027-02-07**. It accepts the same parameters and returns the same response. Migrate to `/v1/search`, which is a drop-in replacement.
</Warning>
